package com.example.demo.controllers;

@RestController
public class StatusController {

    @GetMapping("/status")
    public ResponseEntity<String> getStatus() {
        // Check if backend is ready, return success if ready
        return ResponseEntity.ok("Backend is ready");
    }
}
